# Copy to `env.secret` (git-ignored) and fill in.
#
# A reusable, ephemeral auth key from the coordination server at
# https://sdn.omikron.dev — ephemeral so the node disappears again when the
# stack is torn down instead of piling up dev entries.
TS_AUTHKEY=
