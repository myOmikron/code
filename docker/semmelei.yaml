# Consumed by release-docker.yaml: pushing a tag semmelei/v1.2.3 builds and
# publishes each entry as ghcr.io/<owner>/semmelei/<key>:v1.2.3 and :latest.
dockerfiles:
  # frontend
  nginx: frontend/semmelei
  # services
  webserver: services/semmelei
