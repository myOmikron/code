# Consumed by release-docker.yaml: pushing a tag bnv-manager-v2/v1.2.3 builds
# and publishes each entry as ghcr.io/<owner>/bnv-manager-v2/<key>:v1.2.3
# and :latest.
dockerfiles:
  # frontend
  nginx: frontend/bnv-manager-v2
  # services
  webserver: services/bnv-manager-v2
